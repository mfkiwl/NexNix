# libk.cfg - contains libk configuration values
# SPDX-License-Identifier: ISC

:LIBK
# Standard C Flags
CFLAGS = "-ffreestanding"
CFLAGS_i386 = "-march=i386 -mgeneral-regs-only"
CFLAGS_x86_64 = "-mcmodel=large -mgeneral-regs-only -mno-red-zone"
